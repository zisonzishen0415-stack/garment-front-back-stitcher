# GarmentStitcher 服装正反面拼接工具 — 使用说明书

> 版本：v2.0　|　完全离线运行，无需联网、无需安装 Python

---

## 一、软件简介

GarmentStitcher 是一款**本地离线**的服装样品处理工具。它利用 AI（rembg 人像/服装分割）加轮廓匹配算法，自动识别**正面、反面**两张照片中的服装区域，裁剪后拼接成 **1:1 正方形**输出图，并支持人工微调。

主要用途：电商上架、服装打样归档等需要「正反面合一张方图」的场景。

---

## 二、系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| 内存 | 建议 8 GB 及以上 |
| 磁盘空间 | 安装后约 530 MB |
| 网络 | **不需要**（模型已内置，全程离线） |
| Python | **不需要**（已打包为独立程序） |

---

## 三、安装方式（二选一）

### 方式一：安装版（推荐）

1. 双击 `GarmentStitcher_Setup.exe`
2. 按提示完成安装（默认安装到 `C:\Program Files\GarmentStitcher`）
3. 安装程序会自动创建**桌面**和**开始菜单**快捷方式
4. 若系统缺少 VC++ 运行库，安装程序会自动补装

### 方式二：免安装便携版

1. 解压 `GarmentStitcher_2.0.zip`
2. 进入解压出的 `GarmentStitcher` 文件夹
3. 双击 `GarmentStitcher.exe` 即可运行

> ⚠️ 便携版**必须整体解压**（`GarmentStitcher.exe` 和 `_internal` 文件夹必须在同一目录），不能只拷贝 exe 文件。

---

## 四、快速开始

1. **启动程序**：首次启动会自动加载 AI 模型，状态栏显示「模型加载中…」，几秒后变为「模型就绪」。
2. **准备图片**：把同一件衣服的**正面、反面**照片放在同一个文件夹里，文件名按顺序命名（如 `01_正面.jpg`、`01_反面.jpg`、`02_正面.jpg`、`02_反面.jpg`）。
3. **选择文件夹**：点击工具栏文件夹图标（或输入路径后回车）。
4. **点击「AI 处理」**：程序按顺序逐个处理，处理完一组立刻可以审核，不用等全部完成。
5. **审核与调整**：对识别不准的框进行拖动、旋转微调。
6. **导出**：点「导出」输出当前一组，或点「批量」一次性输出全部。

---

## 五、文件配对规则

- 程序将文件夹内的图片**按文件名排序**，**两两配对**：
  - 第 1、2 张 → 第 1 组
  - 第 3、4 张 → 第 2 组
  - 依此类推……
- 每组中：**前一张 = 正面**，**后一张 = 反面**。
- 若识别反了，可在界面中用「交换」按钮或快捷键 `X` 互换正反面。

**支持的图片格式**：JPG / JPEG / PNG / BMP / TIFF
**建议分辨率**：单张 ≤ 4080px，过大可能拖慢界面。

---

## 六、界面说明

顶部工具栏按钮：

| 按钮 | 功能 |
|---|---|
| **AI 处理** | 开始对全部图片进行 AI 识别（流式，完成一组即可审） |
| **◀ / ▶** | 上一组 / 下一组 |
| **重置AI** | 放弃手动调整，恢复为 AI 自动识别的框 |
| **液化** | 打开液化工具，微调拼接图细节 |
| **导出** | 导出当前这一组 |
| **批量** | 一次性导出全部已处理图片 |
| **关于**（右上角） | 显示软件信息 |

左半区为**预览**，右半区为**编辑**（可拖动绿色选框）。

---

## 七、鼠标操作（编辑区）

| 操作 | 效果 |
|---|---|
| 拖动选框**四角** | 调整选框大小 |
| 拖动选框**边中点** | 单边缩放 |
| 拖动选框**内部** | 移动选框 |
| 在选框**外、靠近角点**拖动 | **旋转**选框（绕中心） |
| 按住 Shift 再旋转 | 以 15° 为步进吸附 |
| 滚动滚轮 | 缩放视图 |
| 按住中键 / 右键拖动 | 平移视图 |

---

## 八、键盘快捷键

| 按键 | 功能 |
|---|---|
| `←` / `→` | 上一组 / 下一组 |
| `E` 或 `S` | 导出当前组 |
| `X` | 交换正反面 |
| `R` | 旋转角度归零 |
| `F` | 编辑区适配窗口 |
| `,` | 逆时针旋转 0.5° |
| `.` | 顺时针旋转 0.5° |
| `F1` | 关于对话框 |

---

## 九、液化工具（可选）

点击「液化」会生成当前组的全分辨率拼接预览，并打开液化编辑窗口：

- 用画笔在图上拖动，进行 PS 风格的局部变形微调；
- 支持撤销（最多 50 步）；
- 点「应用」后，效果会保存到输出目录，并替换当前预览，直到切换到其他组。

---

## 十、输出说明

- 导出结果统一保存到输入文件夹下的 **`审核输出`** 子目录。
- 输出为 **PNG** 格式，文件名与每组的**正面图片**同名。
- 输出为 **1:1 正方形**（正反面拼接，正面靠右、反面靠左）。
- 每次手工调整会自动保存到输入目录下的 **`annotations.json`** 文件（约 0.8 秒防抖），下次打开该文件夹时会优先读取手工标注。

---

## 十一、常见问题（FAQ）

**Q1：提示「模型加载中」很久没反应？**
首次启动加载模型需要几秒到十几秒，属正常现象。若超过 1 分钟，请检查安装目录中 `_internal/models/` 是否存在 `u2net.onnx`、`u2net_finetuned.onnx` 等文件。

**Q2：AI 识别的框不准怎么办？**
用鼠标直接拖动/旋转选框进行修正即可，手动修正后会自动保存。

**Q3：正反面识别反了？**
按 `X` 键或在界面点「交换」即可。

**Q4：输出的图是方图吗？**
是的，最终输出统一为 1:1 正方形。

**Q5：新电脑上能直接运行吗？**
可以。程序完全自包含（含模型和运行库），安装版会自动处理 VC++ 运行库，便携版解压即用，均无需联网。

**Q6：可以处理多少张图？**
按文件名排序两两成对即可，没有数量限制。批量导出会按顺序全部处理。
